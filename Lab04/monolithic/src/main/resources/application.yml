spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/monolithic_db
    username: admin
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    user:
      name: admin
      password: admin123